const addContactQuery = (user, content) => {
  // DB Query goes here
  return 1
}

const editContactQuery = (user, content) => {
  // DB Query goes here
  return 0
}

const deleteContactQuery = (user, content) => {
  // DB Query goes here
  return -1 
}

export default {
  addContactQuery,
  editContactQuery,
  deleteContactQuery
}